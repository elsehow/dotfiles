# Generated by sketchybar's generate-theme.sh on every reload — never tracked.
sketchybar/.config/sketchybar/theme.env

# Per-machine cosmetic workspace labels, written by `ws-label` — runtime state.
sketchybar/.config/sketchybar/workspace-labels.env

# Editor / OS noise
.DS_Store
*.bak
*~
